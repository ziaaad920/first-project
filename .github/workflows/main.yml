on:
      push:
          branches:
              -master



jobs: 
      build:
          runs-on: ubuntu-latest
          steps:
                  - name: 'Test Github actions'
                    run: pwd
                  - name: 'Test Printing'
                    run: echo ziad
            
          